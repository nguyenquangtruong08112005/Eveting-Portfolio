# Logs Directory

This directory contains runtime log files generated by the application.

- `app.log`: Structured application logs in JSONL format.
- `http.log`: Structured HTTP request/response logs in JSONL format.

These log files are ignored by git, but this directory is kept in the repository via the `.gitkeep` file.
